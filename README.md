# Google Photos — Core Experience

NextLeap PM fellowship project. The job is helping people retrieve a photo they still picture but cannot precisely describe — not a better search box.

Prototype: from this folder run `npm run dev` (Photos storefront on http://localhost:3000). Remember is the hypothesis. Search is the keyword baseline.

| Surface | URL |
|---------|-----|
| Photos | http://localhost:3000 |
| Remember | http://localhost:3000/mvp |
| Goa café task | http://localhost:3000/demo/task/task-goa-cafe |
| 10-slide PDF | [docs/deck/NL_GooglePhotos.pdf](docs/deck/NL_GooglePhotos.pdf) |

Copy `Phase-1/.env.example` to `Phase-1/.env` for a live model. Do not commit `.env`.
