# MVP test notes (t01…)

Part 6 return sessions. Representative seed tasks (X-01) from S2 form respondents r01–r03. Scores are from the protocol: keyword Search first, then Remember, unassisted, on the prototype library. Time-to-target was not instrumented — notes say so instead of inventing seconds.
