# Discovery workflow (deck 1-slider)

Collect (Play Store · App Store · Reddit · collect/fixtures) → Keep (drop short / storage-only / SHA-256 dupes; retrieval keyword gate) → Group (≥2 grounded quotes; Q1–Q10; metric node) → Score (`0.4 × impact + 0.4 × feasibility + 0.2 × frequency`) → Gate (generic-search rows flagged; no lock until interviews).
