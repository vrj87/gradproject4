# Phase 4 — Problem definition

```bash
npm run phase4:lock
```

Reads Phase 1 ranking, Phase 2 nomination, and Phase 3 `phase3.json`. Writes the lock, decision tree, and S2 segment contract. Copies the job statement into `phase-5/README.md`.

Storefront: http://localhost:3000/?view=problem · `GET /api/phase4` · `GET /api/problem-definition`

Does not lock while in-segment n &lt; 5 (A-P07). Does not invent interview quotes.
